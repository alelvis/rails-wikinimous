<div class='container'>
  <ul>
  <% @articles.each do |article| %>
    <li><h3><%= link_to article.title, article_path(article.id) %></h3></li>
  <% end %>
  </ul>

  <%= link_to "Add Article", new_article_path, class: 'btn btn-primary' %>
</div>
